services:
  tmflow:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        DEFAULT_APP: TMFlow
        TMFLOW_URL: ${TMFLOW_URL:-}
    image: ghcr.io/collaborativeroboticslab/omron_win_software-tmflow:latest
    network_mode: host